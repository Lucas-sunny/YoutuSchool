# 🐳 Supabase 시작하기 (왕초보 가이드)

코다리 부장입니다! 🫡
Supabase(수파베이스)가 처음이라 낯설 수 있습니다. 쉽게 말해 **"엑셀처럼 편한 온라인 데이터베이스"**라고 생각하시면 됩니다.
우리가 수집한 유튜브 정보들을 저장할 **'창고'**를 만드는 과정입니다. 천천히 따라와주세요!

## 1단계: 가입 및 프로젝트 생성
1.  [Supabase 홈페이지](https://supabase.com/)에 접속합니다.
2.  오른쪽 위 **"Start your project"** 버튼을 누르고, GitHub 아이디로 로그인합니다.
3.  **"New Project"** 버튼을 클릭합니다.
4.  다음 내용을 입력합니다:
    *   **Name**: `YoutuSchool` (원하는 이름 아무거나)
    *   **Database Password**: **이 비밀번호는 꼭 기억해두세요!** (또는 'Generate a password'로 만들고 복사해두기)
    *   **Region**: `Seoul` (Korea, Republic of) - 한국이 제일 빠릅니다!
5.  **"Create new project"** 클릭! (세팅되는데 1~2분 정도 걸립니다. 커피 한 모금 하세요! ☕)

## 2단계: API 키 가져오기 🔑
프로젝트가 만들어지면, 우리 프로그램이 접속할 수 있는 '열쇠'가 필요합니다.

1.  왼쪽 메뉴 맨 아래 **Project Settings (톱니바퀴 아이콘)** 클릭.
2.  **API** 메뉴 클릭.
3.  여기서 두 가지 값을 찾아 메모장에 복사해두세요:
    *   **Project URL**: `https://...supabase.co` 형태
    *   **Project API keys** (두 개 다 필요합니다):
        *   `anon` (public): 프론트엔드(웹사이트)용
        *   `service_role` (secret): **크롤러(데이터 수집)용** (이건 절대 남에게 보여주면 안 됩니다!)

## 3단계: 테이블 만들기 (SQL 실행) 🛠️
데이터를 담을 '틀'을 만들어야 합니다. 제가 다 짜드렸으니 복사-붙여넣기만 하세요!

1.  왼쪽 메뉴에서 **SQL Editor (터미널 아이콘 `>_`)** 클릭.
2.  **"New query"** 클릭.
3.  아래 내용을 싹 복사해서 붙여넣으세요:

```sql
-- 게시글(Posts) 테이블 생성
create table public.posts (
  id uuid primary key default gen_random_uuid(),
  title text not null,
  content text,
  subreddit text not null,
  url text,
  author text,
  post_id text unique not null, -- Reddit 글 ID (중복 방지)
  upvotes int default 0,
  comment_count int default 0,
  created_at timestamptz default now(),
  crawled_at timestamptz default now()
);

-- 누구나 데이터를 볼 수 있게 허용 (읽기 전용)
alter table public.posts enable row level security;
create policy "Public posts are viewable by everyone"
  on public.posts for select
  using ( true );

-- 크롤러가 데이터를 넣을 수 있게 허용 (Service Role 키 사용 시 자동 통과되지만 명시적으로 정책 확인)
-- (기본적으로 Service Role 키는 모든 권한이 있으므로 추가 설정 불필요)
```

4.  오른쪽 아래 **"Run"** 버튼 클릭!
    *   "Success"라고 뜨면 성공입니다. 🎉

## 4단계: 내 컴퓨터에 키 입력하기
이제 아까 복사한 키를 우리 프로젝트에 입력해줘야 합니다.

1.  **크롤러 설정**:
    *   `crawler/.env.example` 파일을 복사해서 `crawler/.env` 파일을 만드세요.
    *   `SUPABASE_URL`에 **Project URL** 붙여넣기.
    *   `SUPABASE_KEY`에 **service_role 키** 붙여넣기. (중요! anon 키 아님!)
    *   `REDDIT_...` 키들은 Reddit에서 받은 걸 넣으세요.

2.  **웹사이트 설정**:
    *   `frontend/.env.example` 파일을 복사해서 `frontend/.env` 파일을 만드세요.
    *   `NEXT_PUBLIC_SUPABASE_URL`에 **Project URL** 붙여넣기.
    *   `NEXT_PUBLIC_SUPABASE_ANON_KEY`에 **anon 키** 붙여넣기.

---
여기까지 하시고 **"설정 완료!"** 라고 외쳐주세요! 코다리가 대기 중입니다! 🫡
